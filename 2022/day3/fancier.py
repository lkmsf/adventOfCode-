import string
import sys

# inputFile = "test.in" if len(sys.argv) > 1 else "data.in"

# with open(inputFile) as f:
#     inp = f.readlines() 
# inp = [x.strip() for x in inp]

# inp = [(inp[i], inp[i+1], inp[i+2]) for i in range(0, len(inp), 3)]

# repeated = [set.intersection(*map(set, inp[i:i + 3])) for i in range(0, len(inp), 3)]
# print(repeated)
# repeated = [a for a, in repeated]

# score = sum((string.ascii_lowercase + string.ascii_uppercase).index(elem) + 1 for elem in repeated)

# print(score)
